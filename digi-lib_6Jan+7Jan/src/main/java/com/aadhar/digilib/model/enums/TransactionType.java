package com.aadhar.digilib.model.enums;

public enum TransactionType {

    // now what all type of Transactions we can have have

    ISSUE,
    RETURN

    // anything else ??
    // NO . lets go back to our Transaction entity class
}
